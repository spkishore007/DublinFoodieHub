<% if current_order.order_status_id == 2 %>
<%= link_to " CART[0] (0)", cart_path, class: "btn btn-info" %>
  <% else %>
<%= link_to " CART[#{current_order.order_items.size}] ( #{number_to_currency(current_order.subtotal,unit: "£")} )", cart_path, class: "btn btn-info" %>
<% end %> 
